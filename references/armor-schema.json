{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": { "type": "string" },
      "description": { "type": "string" },
      "defense": { "type": "number", "minimum": 0 },
      "defenseThreshold": { "type": "number", "minimum": 0 },
      "movementSpeedPenalty": { "type": "number", "minimum": 0 },
      "weight": { "type": "string", "enum": ["light", "heavy"] },
      "rarity": { "type": "string", "enum": ["common", "rare", "epic", "legendary"] },
      "requirements": { "type": "string" }
    },
    "minProperties": 8,
    "additionalProperties": false
  }
}
